import React from "react";

interface Props {
  className?: string;
  amount?: string | number;
  usdValue?: string | number;
  formattedAmount?: string | number;
}

const WalletRow: React.FC<Props> = (props) => {
  return <div></div>;
};

export default WalletRow;
